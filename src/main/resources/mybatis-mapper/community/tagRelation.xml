<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.freetalk.mapper.community.TagRelationMapper">

    <insert id="addTagRelation"  parameterType="java.util.HashMap" >
        insert into tr(ht_id,c_key) values (#{ht_id},#{c_key});
    </insert>

</mapper>