<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.freetalk.oauth2.mapper.TagRelationMapper">

    <insert id="insertTagRelation" parameterType="Long">
        INSERT INTO tr
            (ht_id,c_key)
        VALUES
            (#{ht_id},#{c_key})
    </insert>


</mapper>
